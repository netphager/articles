module.exports = new (function() {
    var that = this;

    this.init = function() {
        // loading modules from modules array
        // var module = require(config.appDir+config.controllers[i]);
        // module.app = that;
        // module.init();
    }

});